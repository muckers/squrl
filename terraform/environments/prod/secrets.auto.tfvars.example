# secrets.auto.tfvars - Local secrets (DO NOT COMMIT)
#
# This file contains environment-specific secrets and AWS resource identifiers.
# Terraform automatically loads *.auto.tfvars files.
#
# SETUP:
# 1. Copy this file to secrets.auto.tfvars (without .example)
# 2. Fill in your actual AWS resource values
# 3. The secrets.auto.tfvars file is gitignored and will never be committed
#
# VALUES:
# - Get these after deploying or from AWS Console
# - These values override defaults in terraform.tfvars

# ACM Certificate ARN for CloudFront HTTPS
# Required: Create in AWS Certificate Manager (us-east-1 region only)
# Get from: AWS Console -> Certificate Manager -> Certificates
acm_certificate_arn = "arn:aws:acm:us-east-1:YOUR_ACCOUNT_ID:certificate/YOUR_CERT_ID"

# You can add other sensitive values here as needed:
# admin_email = "your-actual-admin@example.com"
