# Squrl Environment Configuration
#
# ⚠️  IMPORTANT SECURITY NOTICE ⚠️
# 1. Copy this file to .env and fill in your actual values
# 2. NEVER commit the .env file to version control
# 3. The .env file is in .gitignore - keep it that way
# 4. Never share your .env file or paste it in public channels
# 5. Use different credentials for dev/staging/production

# ============================================================================
# AWS Configuration
# ============================================================================

# AWS Region for deployment
AWS_REGION=us-east-1

# AWS Profile to use (optional, defaults to default profile)
# AWS_PROFILE=your-profile-name

# ============================================================================
# API Configuration
# ============================================================================

# Primary API endpoint (production)
# SQURL_API_ENDPOINT=https://squrl.pub

# Staging API endpoint
# Format: https://YOUR_API_ID.execute-api.REGION.amazonaws.com/v1
# Example: SQURL_STAGING_ENDPOINT=https://abcd1234.execute-api.us-east-1.amazonaws.com/v1
SQURL_STAGING_ENDPOINT=

# Direct API Gateway endpoint for testing (includes /v1 path)
# Get this from: terraform output -raw api_gateway_url
SQURL_API_ENDPOINT=

# ============================================================================
# CloudFront Configuration
# ============================================================================

# CloudFront Distribution ID for cache invalidation
# Get this from: terraform output -raw cloudfront_distribution_id
# or from AWS Console -> CloudFront -> Distributions
CLOUDFRONT_DISTRIBUTION_ID=

# ============================================================================
# Deployment Configuration
# ============================================================================

# S3 bucket for static web UI hosting
S3_BUCKET=squrl-web-ui-dev

# Path to web-ui directory (relative to project root)
WEB_UI_DIR=./web-ui

# ============================================================================
# Terraform Configuration
# ============================================================================

# ACM Certificate ARN for HTTPS (must be in us-east-1 region)
# Create in AWS Console -> Certificate Manager
# Example: arn:aws:acm:us-east-1:123456789012:certificate/abc-123-def
# TF_VAR_acm_certificate_arn=

# Admin email for alerts and notifications
# TF_VAR_admin_email=admin@example.com

# ============================================================================
# Local Development
# ============================================================================

# LocalStack configuration (for local development)
# AWS_ENDPOINT_URL=http://localhost:4566
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test

# ============================================================================
# Notes
# ============================================================================
# 1. Never commit .env file with real values to version control
# 2. ACM certificates for CloudFront must be created in us-east-1 region
# 3. Get CloudFront Distribution ID after running: terraform apply
# 4. Get API Gateway URL from: terraform output
# 5. For production deployment, set up proper AWS credentials using AWS CLI
